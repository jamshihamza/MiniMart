{
  "name": "minimart",
  "version": "0.0.0",
  "private": true,
  "description": "MiniMart offline-first retail platform monorepo",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22 <25",
    "pnpm": ">=12 <13"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:boundaries": "node infra/tooling/check-dependency-boundaries.mjs",
    "check:authority": "node infra/tooling/check-frozen-manifests.mjs",
    "check:protected-tests": "node infra/tooling/check-protected-test-ownership.mjs",
    "test:boundaries": "node --test infra/tooling/check-dependency-boundaries.test.mjs",
    "test:authority": "node --test infra/tooling/check-frozen-manifests.test.mjs",
    "test:protected-tests": "node --test infra/tooling/check-protected-test-ownership.test.mjs",
    "test:database": "pnpm --filter @minimart/database test",
    "test:service-runtime": "pnpm --filter @minimart/service-runtime test",
    "test:pos-shell": "pnpm --filter @minimart/pos-terminal test",
    "check:rust": "cargo fmt --all --check && cargo check --workspace --locked && cargo clippy --workspace --all-targets --locked -- -D warnings && cargo fmt --manifest-path apps/pos-terminal/src-tauri/Cargo.toml --check && cargo check --manifest-path apps/pos-terminal/src-tauri/Cargo.toml --locked && cargo clippy --manifest-path apps/pos-terminal/src-tauri/Cargo.toml --all-targets --locked -- -D warnings",
    "ci": "prettier --check . && eslint . && node infra/tooling/check-frozen-manifests.mjs && node --test infra/tooling/check-frozen-manifests.test.mjs && node infra/tooling/check-protected-test-ownership.mjs && node --test infra/tooling/check-protected-test-ownership.test.mjs && node infra/tooling/check-dependency-boundaries.mjs && node --test infra/tooling/check-dependency-boundaries.test.mjs && tsc -b --pretty false",
    "validate:phase0": "pnpm run ci && pnpm run test:database && pnpm run test:service-runtime && pnpm run test:pos-shell && pnpm --filter @minimart/pos-terminal build && pnpm run check:rust"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.5.2",
    "eslint": "^10.11.0",
    "globals": "^16.4.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.1"
  }
}
