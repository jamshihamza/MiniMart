# BRD source status

The available BRD source artifact is `MiniMart_BRD_v0.1.md`.

It is included for business-context/reference traceability, but it is
**not relabelled as v1.0 FROZEN** because the original generated
artifact carries v0.1 status. For implementation, the frozen FRS/NFRS
v1.0 and downstream frozen authorities govern where there is any
conflict.

Do not silently promote or rewrite the BRD status.
