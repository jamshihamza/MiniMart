# MiniMart Stable Problem Code Registry v1.0
The machine-readable authority is `problem-codes-v1.0.json`. CI requires exact set equality between this registry and every OpenAPI `ProblemCode` enum. `INVALID_REQUEST` is the stable 400 code for malformed request envelopes not represented as field validation errors.
