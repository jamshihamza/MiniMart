# MiniMart Development Backlog v0.1

**Status: IMPLEMENTATION BACKLOG BASELINE --- NOT CODE**

Built from the frozen BRD, FRS/NFRS, Domain Model, Database Model v1.2,
Architecture, API Contracts and UI Specification v1.0.

-   Epics: **12**
-   Backlog items: **115**
-   Phase 0 technical-foundation items: **12**
-   Frozen UI screens represented: **88 / 88**

The first implementation objective is deliberately small:

**Store Node starts → PostgreSQL connects → POS desktop starts → POS
calls real `/health` → UI shows "Store Node Online" → tests/CI pass.**

Do not begin with a full POS feature build.
