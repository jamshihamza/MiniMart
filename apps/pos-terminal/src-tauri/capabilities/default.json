{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main",
  "description": "POS shell window with only the fixed Store Node status command",
  "windows": ["main"],
  "permissions": []
}
